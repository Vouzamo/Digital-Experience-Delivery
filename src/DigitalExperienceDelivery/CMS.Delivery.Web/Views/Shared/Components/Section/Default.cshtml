@model SectionModel

<section class="section">
    <h2>@Model.Title</h2>
    <div class="inner">
        @foreach (var rendering in Model.Rendering.Renderings)
        {
            @Html.Partial("_Rendering", rendering)
        }
    </div>
</section>