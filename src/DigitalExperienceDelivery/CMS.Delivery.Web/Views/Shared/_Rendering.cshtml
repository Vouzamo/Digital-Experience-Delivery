@model IEmbeddedRendering

<!-- Rendering component @Model.Content.Id with template @Model.Template.Id -->
@if (Model.Template.TryData<RenderingTemplateModel>(out RenderingTemplateModel template))
{
    await Component.InvokeAsync(template.ViewComponent, Model);
}